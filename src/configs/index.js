import { PATH } from './paths';
import { DRAWER_MENU_LABEL } from './drawerMenu';
import { ROLE } from './roles';

export const THEMES = {
  LIGHT: 'light',
  DARK: 'dark',
};

export const LANGUAGE = {
  ENGLISH: 'en',
  VIETNAMESE: 'vn',
};

export { PATH, DRAWER_MENU_LABEL, ROLE };
